/// ENUM: UnitSystem
/// PURPOSE: Defines the supported measurement systems for engineering calculations.
enum UnitSystem {
  /// Metric system (mm, m, N, kN, kg).
  metric,

  /// Imperial system (inch, ft, lbf, kip, lb).
  imperial,
}
